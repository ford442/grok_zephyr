# ignore node modules and distribution directories
node_modules/
dist/

# typical other ignores
*.log
.env
.DS_Store

# WASM debug profile (release artifacts live in public/)
native/out/
# clangd database, regenerated by native/build.sh (absolute paths)
native/compile_commands.json

# Playwright
test-results/
playwright-report/
blob-report/
tests/visual/diffs/
