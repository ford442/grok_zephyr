/** Shell-density heat overlay — one marker per occupied hash cell, coloured by WGSL source: ./conjunctionDensity.wgsl */
import source from './conjunctionDensity.wgsl';

export const CONJUNCTION_DENSITY_SHADER = source;
