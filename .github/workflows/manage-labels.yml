name: Manage Labels

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/manage-labels.yml'
      - 'scripts/label-config.json'
      - 'scripts/update_labels_requests.py'

jobs:
  manage-labels:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Update labels
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 scripts/update_labels_requests.py
